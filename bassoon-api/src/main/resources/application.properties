server.port=8080

# Groq API key (obter gratuitamente em https://console.groq.com/keys)
groq.api.key=${GROQ_API_KEY:}

# CORS está definido na anotação @CrossOrigin do RecommendationController (origens 5173 e 3000)
